#import "../../dbl"

module addmethods;

extension AddMethod extends dbl SimpleStatement {
	start AM;
	AM -> "extend" classRef:$$Class "with" "method" method:Function;
}

semantics for AddMethod {
	expand method after classRef;
}