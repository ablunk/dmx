#import "../stdx"

module foreach_test {

class A {}

void main() {
	ArrayList alist = new ArrayList;
	alist.add(new A);
	alist.add(new A);
	
	foreach (A a in alist) {
		print alist.indexOf(a) + "\n";
	}
}

}