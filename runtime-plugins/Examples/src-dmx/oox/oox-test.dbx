#import "oox"
#import "../dbl"

module ooxTest {

oox extension A extends dbl SimpleStatement {
	start A;
	A -> "ax" s:STRING ";";
}

// below code would replace the oox extension if dynamic extensions were implemented:

genA;
//class A { string s; }

extension A extends dbl SimpleStatement {
	start A;
	A -> "ax" s:STRING ";";
}
semantics {
	expand "A extensionInstanceDblObject = new A;";
	expand "extensionInstanceDblObject.s = \"" s "\";";
	expand "print extensionInstanceDblObject.s;";
}

void main() {
	print "Hello";
}

}