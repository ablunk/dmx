#import "ebnf"

module EbnfSample {

	extension ExtensionUsingEbfn extends dbl Statement {
		start Forever;
		Forever -> "forever" "{" (stms:list(Statement))* "}";
	}

	extension TestX extends dbl Statement {
		start Test;
		Test -> "testx" ("opt" ("n")? | "x" | "a")+;
	}
	
	// .. -> .. isControl ?= "control"
	// .. -> .. isControl : "control"
	
	void main() {
		btest;
		btest on;
	}

}