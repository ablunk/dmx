#import "../dbl"
#import "../stdlib"

module stdx_forever {

extension Forever {
	SimpleStatement -> Forever;
	Forever -> "forever" body:LocalScopeStatement;
}
semantics {
	expand "while (true) " body;
}

}