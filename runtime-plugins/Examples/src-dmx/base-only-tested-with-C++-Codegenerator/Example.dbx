#import "forever"

module simulation{
	
	active class Timer{
		new(){
			//print self+" constructed \n";
		}
		actions{
			yield;
		}
	}
	void main(){
		for(int i = 0; i < 2000; i = i+1){
			Timer t = new Timer();
			activate t;
		}
		yield;
		forever{
			print self+" ENDE \n";
		}
	}
}